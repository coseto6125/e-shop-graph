[package]
name = "esg-extract"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
esg-core = { workspace = true }
scraper = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
rayon = { workspace = true }
anyhow = { workspace = true }
memmap2 = { workspace = true }

# Used only by integration tests for cross-source schema parity: build the
# extracted graph through the same rkyv serialize/access path the production
# read path takes, then run Cypher queries against it.
[dev-dependencies]
rkyv = { workspace = true }
